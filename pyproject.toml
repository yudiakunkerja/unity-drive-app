[project]
name = "pnm-reporting"
version = "1.0.0"
requires-python = ">=3.11"

[tool.vercel]
entrypoint = "api/upload-site.py"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
